# Python cache and compiled files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
dist/
build/
*.whl

# Virtual environments
.venv/
venv/
ENV/
env/

# Environment files (will be mounted as volumes)
.env
.env.*
!backend/bot/.env.example

# Root .env for Docker Compose (not needed in image)
/.env

# Log files (will be mounted as volumes)
logs/*.log
!logs/.gitkeep

# Git files
.git/
.gitignore
.gitattributes

# IDE and editor files
.vscode/
.idea/
.cursor/
*.swp
*.swo
*~

# Documentation
docs/
*.md
!README.md

# Tests (backend/bot specific)
backend/bot/tests/
backend/bot/pytest.ini
backend/bot/.pytest_cache/
backend/bot/htmlcov/
backend/bot/.coverage

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Docker files (to avoid recursion)
Dockerfile*
docker-compose*.yml
.dockerignore

# CI/CD
.github/
.gitlab-ci.yml

# Frontend (not needed in backend image)
frontend/

# Backend API (not needed in bot image)
backend/api/

